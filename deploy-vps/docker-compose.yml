services:
  app:
    image: nginx:alpine
    container_name: bossiz-app
    restart: unless-stopped
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
